version: "3.8"

services:
  nginx:
    image: nginx
    ports:
      - "81:80"
    volumes:
      - "./nginx/nginx.conf:/etc/nginx/nginx.conf"
      - "./static_files:/usr/share/nginx/html"

    env_file:
      - "./.env"